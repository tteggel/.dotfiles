{
  "model": "opus[1m]",
  "autoCompactWindow": 512000,
  "permissions": {
    "defaultMode": "auto"
  },
  "skipAutoPermissionPrompt": true,
  "autoMode": {
    "environment": [
      "$defaults",
      "**Primary use of Claude Code**: software development — this repo is a personal NixOS-WSL dotfiles bootstrap (seed/thixos/yoloixos identities)",
      "**Repository visibility**: PUBLIC — tteggel/.dotfiles (github.com:tteggel/.dotfiles.git) — any push there is publishing",
      "**Trusted repo**: /home/thom/src/github.com/tteggel/.dotfiles and its origin remote (github.com:tteggel/.dotfiles.git) — PUBLIC, so only this repo's own work belongs here; confidential/secret material is never cleared into it by visibility alone",
      "**Source control**: the trusted repo (tteggel/.dotfiles) and its origin remote only — no additional orgs configured",
      "**Default / protected branches**: master (default); no rulesets or protected branches listed via gh",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar. Note: templates/nodejs/.envrc flagged by filename scan as sensitive-looking (template file, verify contents before treating as live secret); CI references GITHUB_TOKEN (deploy key existence only, not its value)",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "**Org-specific CLIs**: None configured — repo-local tools observed in this project: zellij, fd, nix, out (routine, low-frequency)",
      "**Routine operations**: commands and file operations under the /home/thom/src/github.com/tteggel/.dotfiles/ prefix are routine for this user"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "claude-statusline"
  }
}
